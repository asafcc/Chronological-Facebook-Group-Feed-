# Chronological Facebook Group Feed 🕑

Setting the default sorting posts option in Facebook groups to a chronological order instead of by "New Activity".
Currently working only when opened in a **new tab**.
